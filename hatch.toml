[envs.default]
dependencies = [
  "coverage[toml]",
  "pytest"
]
[envs.default.scripts]
test = "pytest {args:test}"
test-cov = "coverage run -m pytest {args:test}"
cov-report = [
  "- coverage combine",
  "coverage report --show-missing",
]
cov = [
  "test-cov",
  "cov-report",
]

[[envs.all.matrix]]
python = ["3.12"]

[envs.lint]
detached = true
dependencies = [
  "black>=22.10.0",
  "mypy>=0.991",
  "ruff>=0.0.166",
]
[envs.lint.scripts]
typing = "mypy --install-types --non-interactive {args: vaser }"
style = [
  "ruff check {args:vaser}",
  "black --check --diff {args:vaser}",
]
fmt = [
  "black {args:vaser}",
  "ruff check --fix {args:vaser}",
  "style",
]
all = [
  "style",
  "typing",
]
